[general]
name=Topology Split
qgisMinimumVersion=4.0
description=Line topology toolkit: node a layer, resolve dangles, collapse pseudo-nodes.
about=Adds a Processing provider with tools to clean and build the topology of a line
    layer: (1) Topology split — node the layer at every crossing (X) and endpoint-touch
    (T), extending dangles along their direction up to a tolerance; (2) Resolve dangles —
    directionally extend undershoots and trim overshoots; (3) Collapse pseudo-nodes —
    merge chains of lines through degree-2 nodes into single lines. Output is single-part,
    geometry and attributes preserved. Built for QGIS 4.0 (Qt6 / PyQt6).
version=0.1.0
author=oksion
email=oksion@gmail.com

; Plugin homepage / bug tracker / source (fill in when published)
homepage=https://example.com/topology-split
tracker=https://example.com/topology-split/issues
repository=https://example.com/topology-split

; Comma separated, no spaces after commas
tags=topology,split,noding,network,line,vector,processing,geometry,snap,dangle,overshoot,undershoot,pseudonode,dissolve,clean

category=Vector
icon=resources/icon.svg

; This plugin ships a Processing provider (no classic GUI menu entry).
hasProcessingProvider=yes

; Mark as experimental until the algorithm is validated on real data.
experimental=True
deprecated=False

; QGIS 4.0 is Qt6-only; the pre-release `supportsQt6` flag was removed and must NOT be set.
changelog=See CHANGELOG.md
